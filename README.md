# nhatvibecoders
CBC Hackathon SP26

## Frontend

React Native (Expo) lives in `frontend/`.

```bash
cd frontend
npm start
```

## Backend

Flask + Postgres lives in `backend/` (see `backend/README.md`).
